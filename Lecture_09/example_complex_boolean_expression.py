
# just demonstrating how code can start to get complicated / difficult to
# read and maintain as it gets more complex.

if x > 10 or y < 9 and z == m:
    print("Yay!")
    
# The thing to keep in mind about logical and and logical or:

# For logical and, an expression is only true if both simple expressions are
# true.

# For logical or, an expression is only false if both simple expressions are
# false.